import {describe,expect,it} from "vitest";import {checkPersistentStorage,parseMountInfo} from "./check-persistent-storage.mjs";
const env={MENTOR_REQUIRE_PERSISTENT_STORAGE:"1",MENTOR_PERSISTENT_MOUNT_PATH:"/opt/render/project/src/persistent",MENTOR_DATA_DIRECTORY:"/opt/render/project/src/persistent/data"};
describe("persistent storage startup check",()=>{it("accepts a dedicated durable mount",()=>{const info="1 2 0:1 / / rw - overlay overlay rw\n3 1 0:2 / /opt/render/project/src/persistent rw - ext4 disk rw";expect(parseMountInfo(info)).toContainEqual({mountPoint:"/opt/render/project/src/persistent",fileSystem:"ext4"});expect(()=>checkPersistentStorage(env,info)).not.toThrow();});it("rejects missing and overlay mounts",()=>{expect(()=>checkPersistentStorage(env,"1 2 0:1 / / rw - overlay overlay rw")).toThrow("PERSISTENT_STORAGE_NOT_MOUNTED");expect(()=>checkPersistentStorage(env,"3 1 0:2 / /opt/render/project/src/persistent rw - overlay disk rw")).toThrow("PERSISTENT_STORAGE_NOT_MOUNTED");});});
