"use client";
import { useState } from "react";import { Database,Download,Save,ShieldCheck } from "lucide-react";import { useAppState } from "@/hooks/use-state";import { Loading,Notice,PageHeader } from "@/components/ui";
export default function Settings(){const {data,act}=useAppState();if(!data)return <Loading/>;return <SettingsForm key={JSON.stringify(data.settings)} initial={data.settings} act={act}/>}
function SettingsForm({initial,act}:{initial:Record<string,string>;act:(payload:object)=>Promise<void>}){const [form,setForm]=useState(initial);const [saved,setSaved]=useState(false);function update(key:string,value:string){setForm(prev=>({...prev,[key]:value}))}async function save(e:React.FormEvent){e.preventDefault();await act({action:"saveSettings",settings:form});setSaved(true)}return <div className="mx-auto max-w-5xl"><PageHeader eyebrow="SCR-012 · Contrôle local" title="Paramètres" description="Configurez votre profil, votre examen et les garde-fous de coût sans exposer de secret au navigateur."/>{saved&&<div className="mb-5"><Notice success>Paramètres enregistrés dans la base locale.</Notice></div>}<form onSubmit={save} className="grid gap-6 lg:grid-cols-2"><section className="card p-6"><h2 className="mt-0">Profil d’apprentissage</h2><label className="mb-4 block"><span className="label">Nom affiché</span><input className="field" value={form.displayName??""} onChange={e=>update("displayName",e.target.value)}/></label><label className="mb-4 block"><span className="label">Langue préférée</span><select className="field" value={form.language??"fr"} onChange={e=>update("language",e.target.value)}><option value="fr">Français</option><option value="en">English</option></select></label><label className="block"><span className="label">Date cible de l’examen</span><input type="date" className="field" value={form.examDate??""} onChange={e=>update("examDate",e.target.value)}/></label></section><section className="card p-6"><h2 className="mt-0">Intelligence artificielle</h2><label className="mb-4 block"><span className="label">Fournisseur</span><select className="field" value={form.aiProvider??"Mode local"} onChange={e=>update("aiProvider",e.target.value)}><option>Mode local</option><option disabled>OpenAI (clé serveur requise)</option><option disabled>Gemini (à configurer)</option></select></label><label className="block"><span className="label">Budget quotidien maximal (CAD)</span><input type="number" min="0" step="0.25" className="field" value={form.dailyBudget??"2.00"} onChange={e=>update("dailyBudget",e.target.value)}/></label><div className="mt-4"><Notice>Les clés API doivent être placées dans .env.local. Elles ne sont jamais enregistrées dans l’interface.</Notice></div></section><button className="btn btn-primary lg:col-span-2"><Save size={16}/>Enregistrer les paramètres</button></form><div className="mt-6 grid gap-5 md:grid-cols-3"><div className="card p-5"><Database className="mb-4 text-[var(--primary)]"/><h3>Base locale</h3><p className="text-sm text-[var(--muted-foreground)]">SQLite, journal WAL et intégrité référentielle activée.</p></div><div className="card p-5"><ShieldCheck className="mb-4 text-[var(--primary)]"/><h3>Confidentialité</h3><p className="text-sm text-[var(--muted-foreground)]">Aucun document complet n’est envoyé ou journalisé sans action explicite.</p></div><div className="card p-5"><Download className="mb-4 text-[var(--primary)]"/><h3>Sauvegarde</h3><p className="text-sm text-[var(--muted-foreground)]">Copiez le dossier data et storage lorsque l’application est arrêtée.</p></div></div></div>}
