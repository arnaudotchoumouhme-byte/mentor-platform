"use client";
import { CheckCircle2, Target } from "lucide-react";import { useAppState } from "@/hooks/use-state";import { EmptyState,Loading,Notice,PageHeader } from "@/components/ui";
export default function Weaknesses(){const {data,act}=useAppState();if(!data)return <Loading/>;const active=data.weaknesses.filter(w=>w.status==="active");return <div className="mx-auto max-w-5xl"><PageHeader eyebrow="FEAT-021 · Détection prudente" title="Lacunes" description="Des hypothèses explicables, fondées sur plusieurs observations et reliées à une action concrète."/><div className="mb-5"><Notice>Une lacune n’est jamais présentée comme une certitude. Son niveau de confiance reflète la quantité et la cohérence des données disponibles.</Notice></div>{!active.length?<EmptyState title="Aucune lacune active" detail="Les lacunes résolues restent conservées dans l’historique."/>:<div className="space-y-4">{active.map(w=><article className="card p-6" key={w.id}><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex gap-4"><span className="grid h-11 w-11 place-items-center rounded-xl bg-amber-50 text-amber-700"><Target/></span><div><div className="mb-1 text-xs font-bold text-[var(--muted-foreground)]">{w.subject}</div><h2 className="m-0 text-xl">{w.topic}</h2></div></div><span className="badge">Confiance {w.confidence.toLowerCase()}</span></div><div className="my-5 grid gap-4 md:grid-cols-2"><div className="rounded-xl bg-[var(--muted)] p-4"><div className="label">Cause probable</div><strong>{w.cause}</strong></div><div className="rounded-xl bg-[var(--accent)] p-4"><div className="label">Action recommandée</div><strong>{w.action}</strong></div></div><button onClick={()=>act({action:"resolveWeakness",id:w.id})} className="btn btn-secondary"><CheckCircle2 size={16}/>Marquer comme résolue</button></article>)}</div>}</div>}
