import {NextResponse} from "next/server";import {z} from "zod";import type {UseCase} from "@/application/contracts";import type {PilotIdentity} from "@/application/pilot/pilot-core";import {mapErrorToHttp} from "@/presentation/api/http-error-mapper";import {resolveTraceId} from "@/shared/observability/trace-id";
const id=z.string().uuid();
export function createSessionGet(load:()=>Promise<UseCase<string,unknown>>,identity:()=>Promise<PilotIdentity>,authorize:(sessionId:string,learnerId:string)=>Promise<void>){return async(request:Request,context:{params:Promise<{sessionId:string}>})=>{const traceId=resolveTraceId(request.headers.get("x-trace-id"));const parsed=id.safeParse((await context.params).sessionId);if(!parsed.success)return NextResponse.json({success:false,error:{code:"VALIDATION_ERROR",message:"Identifiant de session invalide.",traceId,retriable:false}},{status:400,headers:{"x-trace-id":traceId}});try{const caller=await identity();await authorize(parsed.data,caller.learnerId);return NextResponse.json(await(await load()).execute(parsed.data),{headers:{"x-trace-id":traceId}});}catch(error){const response=mapErrorToHttp(error,traceId);return NextResponse.json(response.body,{status:response.status,headers:{"x-trace-id":traceId}});}};}
export const GET=createSessionGet(async()=>(await import("@/infrastructure/mcq/server-mcq")).mcqServices.get,async()=>(await import("@/infrastructure/pilot/server-pilot")).requirePilotIdentity(),async(sessionId,learnerId)=>(await import("@/infrastructure/pilot/server-pilot")).pilotOwnership.assertMcqSession(sessionId,learnerId));
