import {describe,expect,it,vi} from "vitest";import {AppError} from "@/shared/errors/app-error";import {createSessionGet} from "./[sessionId]/route";import {createAnswerPost} from "./[sessionId]/answers/route";import {createCompletePost} from "./[sessionId]/complete/route";
const sessionId="11111111-1111-4111-8111-111111111111";const context={params:Promise.resolve({sessionId})};const identity=async()=>({accountId:"account",learnerId:"learner"});const authorize=vi.fn(async()=>{});
describe("MCQ route contracts",()=>{it("reads an owned session",async()=>{const execute=vi.fn(async()=>({session:{sessionId}}));const response=await createSessionGet(async()=>({execute}),identity,authorize)(new Request(`http://local/api/mcq/sessions/${sessionId}`),context);expect(response.status).toBe(200);expect(authorize).toHaveBeenCalledWith(sessionId,"learner");});it("submits an owned answer",async()=>{const execute=vi.fn(async(input)=>input);const response=await createAnswerPost(async()=>({execute}),identity,authorize)(new Request("http://local",{method:"POST",body:JSON.stringify({itemId:"item-1",itemVersion:1,choiceId:"a",durationMs:12})}),context);expect(response.status).toBe(200);});it("completes an owned session",async()=>{const execute=vi.fn(async()=>({status:"COMPLETED"}));expect((await createCompletePost(async()=>({execute}),identity,authorize)(new Request("http://local",{method:"POST"}),context)).status).toBe(200);});it("blocks every operation before use-case execution when ownership is denied",async()=>{const execute=vi.fn();const load=async()=>({execute});const denied=vi.fn(async()=>{throw new AppError({code:"PILOT_ACCESS_DENIED",userMessage:"Accès refusé.",category:"security"});});expect((await createSessionGet(load,identity,denied)(new Request("http://local"),context)).status).toBe(403);expect((await createAnswerPost(load,identity,denied)(new Request("http://local",{method:"POST",body:JSON.stringify({itemId:"item-1",itemVersion:1,choiceId:"a"})}),context)).status).toBe(403);expect((await createCompletePost(load,identity,denied)(new Request("http://local",{method:"POST"}),context)).status).toBe(403);expect(execute).not.toHaveBeenCalled();});it("rejects invalid IDs before loading",async()=>{const load=vi.fn();const invalid={params:Promise.resolve({sessionId:"invalid"})};expect((await createSessionGet(load,identity,authorize)(new Request("http://local"),invalid)).status).toBe(400);expect((await createAnswerPost(load,identity,authorize)(new Request("http://local",{method:"POST",body:"{}"}),invalid)).status).toBe(400);expect((await createCompletePost(load,identity,authorize)(new Request("http://local"),invalid)).status).toBe(400);});});
