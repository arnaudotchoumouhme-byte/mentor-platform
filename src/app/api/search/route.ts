import {NextResponse} from "next/server";import {z} from "zod";import type {PilotIdentity} from "@/application/pilot/pilot-core";import {mapErrorToHttp} from "@/presentation/api/http-error-mapper";import {all} from "@/lib/db";
const schema=z.object({query:z.string().trim().min(2),subject:z.string().optional()});
export function createSearchPost(identity:()=>Promise<PilotIdentity>,query:typeof all=all){return async(request:Request)=>{try{await identity();const parsed=schema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:"Saisissez au moins deux caractères."},{status:400});const term=`%${parsed.data.query}%`;const subject=parsed.data.subject;const rows=subject&&subject!=="Toutes"?query("SELECT id,name,subject,content,status FROM documents WHERE archived=0 AND subject=? AND (name LIKE ? OR content LIKE ?) LIMIT 30",subject,term,term):query("SELECT id,name,subject,content,status FROM documents WHERE archived=0 AND (name LIKE ? OR content LIKE ?) LIMIT 30",term,term);return NextResponse.json({results:rows});}catch(error){const mapped=mapErrorToHttp(error);return NextResponse.json(mapped.body,{status:mapped.status});}};}
export const POST=createSearchPost(async()=>(await import("@/infrastructure/pilot/server-pilot")).requirePilotIdentity());
